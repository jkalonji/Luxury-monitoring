[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    live: hits real external services (network); run with `pytest -m live`
addopts = -m "not live" -q
filterwarnings =
    ignore::DeprecationWarning
